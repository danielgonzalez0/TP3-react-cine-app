import React from 'react';
import Header from '../../components/header/Header';

const Favorites = () => {
    return (
        <div>
            <Header/>
            <h1>Favorites</h1>
        </div>
    );
};

export default Favorites;